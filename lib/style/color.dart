import 'package:flutter/material.dart';

// Palet Warna Biru Modern (Soft & Clean)
const Color backgroundColor1 = Color(
  0xffF5F7FA,
); // Latar belakang utama (Putih keabuan soft)
const Color backgroundColor2 = Color(
  0xffFFFFFF,
); // Latar elemen/kartu (Putih bersih)
const Color primaryColor = Color(
  0xff065CF0,
); // Warna utama (Biru Vivid dari main.dart)
const Color buttonColor = Color(0xff065CF0); // Warna tombol
const Color textColor1 = Color(
  0xff1A1F2B,
); // Teks Judul (Hampir hitam, tidak pekat)
const Color textColor2 = Color(0xff7D8799);       // Teks Deskripsi (Abu-abu soft)